import openpyxlimport pprint as ppbook = openpyxl.open("ПХ3-10.xlsx", read_only=True)sheet = book.activecells = sheet['B1':'C11']# for row in sheet.iter_rows(min_row=2, max_row=20, min_col=2, max_col=4):#     for cell in row:#         print(cell.value,(row), end=' ')#     print()## for row in range(4, sheet.max_row+1):#     Номер_вещества = sheet[row][0].value#     Вещество = sheet[row][1].value#     ПДК = sheet[row][2].value#     Ci = sheet[row][3].value#     ЛПВ = sheet[row][4].value#     Класс_опасности = sheet[row][5].value###     print(Номер_вещества, Вещество, Ci/ПДК, ЛПВ, Класс_опасности)#max_rows = sheet.max_rowsub_dict = {}s_dict = {}for i in range(4, max_rows + 1):    sku = sheet.cell(row=i, column=4).value    a = sheet.cell(row=i, column=6).value    b = sheet.cell(row=i, column=3).value    sku = sku / b    if not sku:        continue    if a not in sub_dict:        sub_dict[a] = [sku]    else:        sub_dict[a].append(sku)pp.pprint(sub_dict)